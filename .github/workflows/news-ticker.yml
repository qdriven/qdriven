name: Tech News Ticker

# Paused while Tech Wire is commented out in README.md.
# Uncomment the original `on` / `jobs` block below to re-enable.
# See docs/live-feeds.md.

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tech News Ticker is paused"

# on:
#   schedule:
#     - cron: "0 */6 * * *"
#   workflow_dispatch:
#
# permissions:
#   contents: write
#
# jobs:
#   update-news:
#     runs-on: ubuntu-latest
#     name: Update Tech News
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#
#       - name: Install dependencies
#         run: |
#           pip install feedparser
#
#       - name: Fetch and Update News
#         run: |
#           python .github/scripts/update_news.py
#
#       - name: Commit and Push
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
#           git add README.md
#           if git diff --staged --quiet; then
#             echo "No changes to commit"
#           else
#             git commit -m "chore: update tech news ticker"
#             git push
#           fi
